# Migration Tooling

Reserved for legacy→native schema/data migration tooling after the production migration contract exists.

No real-data migration tool should be implemented during Phase 0. Future tools must support dry-run, validation/reconciliation, resumability/idempotency where appropriate, explicit source/target versions and rollback/restore procedures.