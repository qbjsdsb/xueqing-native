# Shared Schemas

Reserved for intentionally shared, implementation-neutral schemas such as diagnostic manifest, sync envelope or generated API fixtures.

Do not force Kotlin and C# to share generated client/domain implementations simply because a schema exists.