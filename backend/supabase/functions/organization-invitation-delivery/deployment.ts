// Non-secret, auditable deployment policy for Xueqing V1.
// Region changes are production-topology migrations and must go through Git/PR/CI.
export const DEFAULT_HOSTED_REQUIRED_EDGE_REGION = "ap-southeast-1";
